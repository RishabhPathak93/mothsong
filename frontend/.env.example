# Base URL of the deployed backend (Render). No trailing slash.
# Leave BLANK for local dev to use Vite's /api proxy to http://localhost:4000.
# Example (prod): https://mothsong-api.onrender.com
VITE_API_BASE=
